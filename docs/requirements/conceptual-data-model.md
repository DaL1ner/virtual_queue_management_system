# Концептуальная модель данных
## Virtual Queue Management System

*Версия: 1.2 | Обновлено: 31.03.2026*

---

## Оглавление

- [Концептуальная модель данных](#концептуальная-модель-данных)
  - [Virtual Queue Management System](#virtual-queue-management-system)
  - [Оглавление](#оглавление)
  - [1. Обзор модели](#1-обзор-модели)
  - [2. Сущности](#2-сущности)
    - [User (Пользователь системы)](#user-пользователь-системы)
    - [Role (Роль)](#role-роль)
    - [UserRole (Связь Пользователь-Роль)](#userrole-связь-пользователь-роль)
    - [Queue (Конфигурация очереди)](#queue-конфигурация-очереди)
    - [QueueSession (Сессия очереди)](#queuesession-сессия-очереди)
    - [Ticket (Талон / Запись в очередь)](#ticket-талон--запись-в-очередь)
    - [ServiceType (Тип обслуживания)](#servicetype-тип-обслуживания)
    - [ExecutorState (Состояние исполнителя)](#executorstate-состояние-исполнителя)
    - [ClientSession (Сессия клиента)](#clientsession-сессия-клиента)
    - [EventLog (Журнал событий)](#eventlog-журнал-событий)
  - [3. Связи между сущностями](#3-связи-между-сущностями)
    - [Диаграмма связей](#диаграмма-связей)
    - [Таблица кардинальностей](#таблица-кардинальностей)
  - [4. Бизнес-правила и ограничения](#4-бизнес-правила-и-ограничения)
    - [4.1. Отображение позиций в очереди](#41-отображение-позиций-в-очереди)
    - [4.2. Добавление позиций в очереди](#42-добавление-позиций-в-очереди)
    - [4.3. Приоритетность](#43-приоритетность)
    - [4.4. Статусы талона (Lifecycle)](#44-статусы-талона-lifecycle)
    - [4.5. Конкурентный доступ (Optimistic Locking)](#45-конкурентный-доступ-optimistic-locking)
    - [4.6. Ограничения по сессии клиента](#46-ограничения-по-сессии-клиента)
    - [4.7. Расчёт времени ожидания](#47-расчёт-времени-ожидания)
    - [4.8. Безопасность данных](#48-безопасность-данных)
  - [5. Приложения](#5-приложения)
    - [A. Перечисляемые типы (Enums)](#a-перечисляемые-типы-enums)

---

## 1. Обзор модели

Данная концептуальная модель данных разработана для системы управления виртуальной очередью (Virtual Queue Management System).

---

## 2. Сущности

### User (Пользователь системы)

Хранит учётные данные сотрудников системы (Администраторы, Операторы, Исполнители). Клиенты очереди не создаются в этой таблице, так как системой реализуется анонимный доступ по ФИО.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Логин | login | Текстовый | Уникальный логин для входа в систему |
| Хеш пароля | password_hash | Текстовый | Хешированный пароль (bcrypt/argon2) |
| ФИО | full_name | Текстовый | Полное имя сотрудника |
| Email | email | Текстовый | Контактный адрес электронной почты |
| Активен | is_active | Логический | Флаг активности учётной записи |
| Дата создания | created_at | Дата и время | Дата создания учётной записи |
| Дата обновления | updated_at | Дата и время | Дата последнего обновления |

---

### Role (Роль)

Справочник системных ролей. Определяет права доступа к функциям системы.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Название | name | Текстовый | Отображаемое название роли (напр. "Оператор") |
| Код | code | Текстовый | Системный код для проверки прав (напр. "OPERATOR") |
| Описание | description | Текстовый | Описание полномочий роли |
| Дата создания | created_at | Дата и время | Дата создания роли |

---

### UserRole (Связь Пользователь-Роль)

Устраняет связь «Многие-ко-Многим» между пользователями и ролями. Один пользователь может иметь несколько ролей одновременно.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Пользователь | user_id | Ссылка на User | Ссылка на пользователя |
| Роль | role_id | Ссылка на Role | Ссылка на роль |
| Дата назначения | assigned_at | Дата и время | Дата назначения роли пользователю |
| Назначил | assigned_by | Ссылка на User | Кто назначил роль (Администратор) |

---

### Queue (Конфигурация очереди)

Шаблон очереди. Содержит настройки и правила, которые не меняются в течение сессии. Создаётся Администратором.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Название | name | Текстовый | Отображаемое название очереди |
| Описание | description | Текстовый | Подробное описание очереди |
| Режим распределения клиентов | distribution_mode | Перечисление | MANUAL (ручной) или AUTO (автоматический) |
| Типы услуг включены | is_service_type_enabled | Логический | Требовать ли выбор услуги |
| Приоритеты включены | is_priority_enabled | Логический | Разрешено ли приоритетное обслуживание |
| Активна | is_active | Логический | Флаг активности конфигурации |
| Дата создания | created_at | Дата и время | Дата создания конфигурации |
| Создал | created_by | Ссылка на User | Администратор, создавший очередь |

---

### QueueSession (Сессия очереди)

Конкретный запуск очереди во времени. Позволяет хранить историю работы за разные дни/периоды.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Конфигурация очереди | queue_id | Ссылка на Queue | Ссылка на конфигурацию очереди |
| Статус | status | Перечисление | DRAFT, OPEN, PAUSED, CLOSED |
| Время начала | started_at | Дата и время | Фактическое время начала работы сессии |
| Время завершения | closed_at | Дата и время | Время завершения сессии |
| Текущий номер талона | current_ticket_number | Целочисленный | Счётчик для генерации номеров талонов |
| Число обслуженных клиентов | served_count | Целочисленный | Счётчик обслуженных клиентов |
| Фактическое среднее время обслуживания | avg_service_time_actual | Целочисленный | Рассчитывается автоматически в секундах |
| Общее время обслуживания | total_service_time | Целочисленный | Сумма времени всех обслуживаний с секундах. Рассчитывается автоматически |
| Создал | created_by | Ссылка на User | Администратор, запустивший сессию |
| Дата создания | created_at | Дата и время | Дата создания сессии |

---

### Ticket (Талон / Запись в очередь)

**Ключевая сущность системы.** Представляет клиента в очереди. Содержит всю информацию о процессе обслуживания.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Сессия | queue_session_id | Ссылка на QueueSession | Ссылка на активную сессию очереди |
| Тип обслуживания | service_type_id | Ссылка на ServiceType | Ссылка на тип обслуживания |
| Номер талона | ticket_number | Текстовый | Видимый номер (напр. "А-005"). Рассчитывается на основе текущего номера талона сущности "Сессия очереди" и типа обслуживания |
| Имя клиента | client_name | Текстовый | Имя клиента (для анонимного входа) |
| Фамилия клиента | client_surname | Текстовый | Фамилия клиента |
| Порядок сортировки | sort_order | Числовой (DECIMAL) | Позиция для сортировки в очереди |
| Текущий уровень приоритета | priority_level | Целочисленный | Текущий приоритет данного клиента. Изначально соответствует приоритету типа обслуживания |
| Статус | status | Перечисление | WAITING, CALLED, SERVING, SERVED, SKIPPED, CANCELLED |
| Версия | version | Целочисленный | Для оптимистичной блокировки (конкурентность) |
| Время создания | created_at | Дата и время | Время записи в очередь (не меняется) |
| Время вызова | called_at | Дата и время | Когда клиент был вызван |
| Начало обслуживания | service_started_at | Дата и время | Когда началось обслуживание |
| Завершение обслуживания | service_ended_at | Дата и время | Когда завершилось обслуживание |
| Обслуживающий | served_by_user_id | Ссылка на User | Исполнитель, который обслуживал |
| Сессия клиента | client_session_id | Ссылка на ClientSession | Сессия устройства клиента |
| Причина отмены | cancel_reason | Текстовый | Причина отмены/пропуска (если есть) |

---

### ServiceType (Тип обслуживания)

Справочник типов услуг. Определяет приоритет и плановое время для каждой услуги

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Конфигурация очереди | queue_id | Ссылка на Queue | Ссылка на конфигурацию очереди |
| Название | name | Текстовый | Название услуги (напр. «Приём документов») |
| Системное название | code | Текстовый | Системный код |
| Буква | Letter | Символьный | Буква для формирования номера талона |
| Базовый уровень приоритета | base_priority_level | Целочисленный | 0 - обычный, 1+ - приоритетный |
| Плановое среднее время обслуживания | plan_avg_service_time | Целочисленный | Плановое среднее время обслуживания время для этого типа в секундах |
| Активен | is_active | Логический | Активен ли тип услуги |
| Выделение | is_highlighting | Логический | Выделяется ли клиент с этим типом обслуживания в UI |

---

### ExecutorState (Состояние исполнителя)

Хранит состояние готовности исполнителя в рамках конкретной сессии очереди. Позволяет одному пользователю работать в нескольких очередях с разным статусом (в случае расширения системы).

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Сессия очереди | queue_session_id | Ссылка на QueueSession | Ссылка на сессию очереди |
| Пользователь | user_id | Ссылка на User | Исполнитель услуги |
| Готов | is_ready | Логический | Флаг готовности принимать клиентов |
| Текущий талон | current_ticket_id | Ссылка на Ticket | Талон, который сейчас обслуживается |
| Последнее изменение | last_status_change | Дата и время | Время последнего изменения состояния |
| Обслужено за сессию | served_count | Целочисленный | Счётчик обслуженных клиентов за сессию |

---

### ClientSession (Сессия клиента)

Отслеживает сессию браузера/устройства клиента. Нужно для реализации требования «один активный талон с устройства».

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Отпечаток устройства | device_fingerprint | Текстовый | Идентификатор устройства/браузера |
| Время создания | created_at | Дата и время | Время создания сессии |
| Время истечения | expires_at | Дата и время | Время истечения сессии |
| Активна | is_active | Логический | Флаг активности сессии |
| IP адрес | ip_address | Текстовый | IP-адрес клиента (для аудита) |
| User Agent | user_agent | Текстовый | Информация о браузере/устройстве |

---

### EventLog (Журнал событий)

Хранит историю всех значимых событий в системе. Используется для аудита, аналитики и отладки.

| Русское название | Название (лат.) | Тип данных | Описание |
|------------------|-----------------|------------|----------|
| Сессия очереди | queue_session_id | Ссылка на QueueSession | Контекст сессии очереди |
| Талон | ticket_id | Ссылка на Ticket | Связанный талон (если есть) |
| Актор | actor_user_id | Ссылка на User | Кто совершил действие (или SYSTEM) |
| Тип события | event_type | Перечисление | TICKET_CREATED, CALLED, SERVICE_START, MOVE, и т.д. |
| Время события | timestamp | Дата и время | Дата и время события |
| Детали | details | Текстовый (JSON) | Дополнительные данные события |

---

## 3. Связи между сущностями

### Диаграмма связей

Тут будет ссылка на фото с нотацией чена

### Таблица кардинальностей

| Сущность 1 | Связь | Сущность 2 | Кардинальность |
|------------|-------|------------|----------------|
| User | имеет | UserRole | 1 : N |
| UserRole | относится к | Role | N : 1 |
| Queue | имеет сессии | QueueSession | 1 : N |
| QueueSession | содержит | Ticket | 1 : N |
| QueueSession | имеет статусы исполнителей | ExecutorState | 1 : N |
| Ticket | принадлежит сессии | ClientSession | N : 1 |
| Ticket | обслуживается | User | N : 1 |
| Ticket | имеет тип обслуживания | ServiceType | 1 : 1 |
| Queue | имеет типы услуг | ServiceType | 1 : N |
| QueueSession/Ticket/User | генерирует | EventLog | 1 : N |
| ExecutorState | обслуживает в данный момент | Ticket | 1 : 1 |

---

## 4. Бизнес-правила и ограничения

### 4.1. Отображение позиций в очереди

Атрибут sort_order использует десятичные числа с шагом 1000  

Позиция в очереди определяется путём сортировки всех активных талонов в состоянии ожидания:
  1. Сначала по приоритету (priority_level DESC)
  2. Затем по полю sort_order (ASC)
  3. При равенстве — по времени создания (created_at ASC)

При добавлении нового талона в очередь, его sort_order рассчитывается на основании максимального sort_order среди талонов с таким же приоритетом  

Данный подход в дальнейшем позволяет добиться O(1) перемещение без пересчёта всех записей. При перемещении sort_order конкретного талона пересчитывается как среднее между sort_order соседних талонов. Тем самым при сортировке клиент будет находится в необходимом месте  

Ренормализация выполняется в фоновом режиме когда минимальный интервал < 100

---

### 4.2. Добавление позиций в очереди

Назначение sort_order новому клиенту происходит на основании последнего клиента с таким же приоритетом в очереди. Отображение очереди в таком случае остаётся корректным даже при дублировании порядка (sort_order) среды разных групп приоритетов, так как среди текущей группы приоритетов этот порядок всё ещё остаётся уникален.

---

### 4.3. Приоритетность

Если конфигурацией очереди клиенту не предоставляется выбор типа услуги, то ему автоматически должна присваиваться "базовая услуга", имеющая приоритет 0.  
Если выбор услуги предоставлен, клиент получает приоритет (`Ticket.priority_level`), соответствующий приоритету выбранного типа обслуживания (`ServiceType.base_priority_level`)
При ручном изменении позиции клиента в очереди его приоритет при необходимости должен изменяться в зависимости от новой позиции позиции:
  - Если клиент перемещается в группу клиентов с другим приоритетом, его `Ticket.priority_level` обновляется в соответствии с целевой позицией
  - Изменение приоритета логируется в EventLog как PRIORITY_CHANGED
  - Оба изменения (sort_order + priority_level) выполняются в одной транзакции

---

### 4.4. Статусы талона (Lifecycle)

```
┌──────────┐    вызов    ┌──────────┐   начало    ┌───────────┐
│ WAITING  │ ──────────> │ CALLED   │ ──────────> │ SERVING   │
└──────────┘             └──────────┘             └───────────┘
     │                        │                        │
     │ отмена                 │ пропуск                │ завершение
     ▼                        ▼                        ▼
┌──────────┐             ┌──────────┐             ┌───────────┐
│CANCELLED │             │ SKIPPED  │             │ SERVED    │
└──────────┘             └──────────┘             └───────────┘
```

**Ограничения:**
- Переход из `SERVING` (обслуживается) возможен только в `SERVED` (обслужен)
- Переход из `CALLED` (вызван) возможен в `SERVING` (обслуживается), `SKIPPED` (пропущен) или `WAITING` (возврат)
- Переход из `WAITING` (ожидает) возможен в `CALLED` (вызван) или `CANCELLED` (отменён)

---

### 4.5. Конкурентный доступ (Optimistic Locking)

Поле `Ticket.version` увеличивается при каждом обновлении  
Проверка выполняется через `UPDATE Ticket SET version = version + 1 WHERE id = ? AND version = ?`  
При конфликте вернуть ошибку 409 Conflict, запросить обновление данных

---

### 4.6. Ограничения по сессии клиента

Согласно функциональным требованиям установлено правило - один активный талон на одну ClientSession  
При создании нового талона необходимо:
  1. Найти все активные талоны с этим client_session_id
  2. Перевести их в статус CANCELLED
  3. Создать новый талон
  4. Записать событие в EventLog

Исключение: Талоны в статусе `SERVING`, `SERVED` не аннулируются (логировать предупреждение)

---

### 4.7. Расчёт времени ожидания

Время ожидания каждого клиента рассчитывается как время_ожидания = (`людей_передо_мной` × `среднее_время_обслуживания`) / `активных_исполнителей`.  
Где:
  - людей_передо_мной = COUNT(Ticket WHERE status='WAITING' и позиция < текущей)
  - среднее_время_обслуживания (`avg_service_time_actual`) = `QueueSession.total_service_time_sec` / `QueueSession.served_count`
  - активных_исполнителей = COUNT(ExecutorState WHERE queue_session_id=? AND (is_ready = true OR current_ticket_id IS NOT NULL))
    (*число исполнителей, обслуживающих клиентов в данный момент ИЛИ готовых к обслуживанию*)

До момента завершения обслуживания первого клиента в качестве среднего времени обслуживания используется плановое среднее временя обслуживания каждого клиента в очереди `ServiceType.plan_avg_service_time`, зависящего от типа обслуживания.  

Если активных_исполнителей = 0, отображать время ожидания для одного активного исполнителя  
Кратковременные колебания времени между завершением обслуживания и нажатием «Готов» допустимы  
Обновление `QueueSession.total_service_time_sec` и `QueueSession.served_count` производится автоматически после каждого нового обслуживания  

---

### 4.8. Безопасность данных

| Требование | Реализация |
|------------|------------|
| Пароли | Хеширование bcrypt/argon2 (NFR-S-01) |
| Передача | HTTPS обязательно (NFR-S-02) |
| SQL Injection | Параметризированные запросы (NFR-S-03) |
| XSS | Санитизация всех входных данных (NFR-S-03) |
| Персональные данные | Не отображать Наблюдателю (3.7.2) |

---

## 5. Приложения

### A. Перечисляемые типы (Enums)

**Ticket.status:**
- `WAITING` — Ожидает вызова
- `CALLED` — Вызван, ожидает подтверждения
- `SERVING` — Обслуживается
- `SERVED` — Обслужен успешно
- `SKIPPED` — Пропущен (не явился)
- `CANCELLED` — Отменён клиентом или оператором

**QueueSession.status:**
- `DRAFT` — Черновик, не активна
- `OPEN` — Активна, принимает клиентов
- `PAUSED` — На паузе, не принимает новых
- `CLOSED` — Завершена

**Queue.distribution_mode:**
- `MANUAL` — Оператор вызывает клиентов вручную
- `AUTO` — Система автоматически назначает готовым исполнителям

---
